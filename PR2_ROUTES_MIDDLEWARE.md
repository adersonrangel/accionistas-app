# PR 2: Routes and Middleware

## Changes

- Created Share CRUD routes (`backend/src/routes/share.routes.ts`)
- Created admin-only middleware (`backend/src/middleware/adminOnly.ts`)
- Created percentage validation middleware (`backend/src/middleware/validateSharePercentage.ts`)

## Acceptance criteria

- Admin-only writes to shares
- Routes work with Mongoose models
EOF
